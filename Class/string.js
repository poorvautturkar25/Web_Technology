//old way 
const str1 = "JS";
console.log(str1);

// new way = string interpolation -> This is a good way to print string so now we use this way 
console.log(`Hello My Course Name ${str1}`);

const gameCount = new String("DL");
console.log(gameCount.length);
console.log(gameCount[1]);  // which element at which position 
console.log(gameCount.toUpperCase());
console.log(gameCount.toLowerCase());
console.log(gameCount.charAt(1));  // to see which character is which index 
console.log(gameCount.indexOf("L"));

const newstr2 = "  JS  ";

console.log(newstr2);
console.log(newstr2.trim()); // to remove extra spaces

// Activity --- Use all string options like include, split, 
